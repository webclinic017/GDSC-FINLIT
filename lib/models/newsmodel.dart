class NewsModel {
  String author;
  String title;
  String description;
  String content;
  String publishedAt;
  String url;
  String urlToImage;

  NewsModel({this.author, this.title, this.description, this.content, this.publishedAt, this.url, this.urlToImage});
}